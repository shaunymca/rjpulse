    <div class="intro">
      <div class="row">
        <div class="col-md-7">
          <div class="beta">
            In Beta
          </div>
          <h1>How Data Smart is Your Company?</h1>
          <p class="descrip">Ecommerce is a cutthroat world filled with enormous opportunity and razor thin margins. It’s a world where only the data smart survive.</p>
          <hr/>
          <p class="small">This 10 minute survey will measure your “data smarts” in areas including company culture, testing, key metrics, and processes. See your results benchmarked against other ecommerce companies like you.</p>

        </div>
        <div class="col-md-5">
          <div class="authform">
            <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :role => 'form'}) do |f| %>
              <%= devise_error_messages! %>
              <div class="form-group">
                <%= f.text_field :name, :autofocus => true, :placeholder => 'Name', class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.email_field :email, :placeholder => 'Email', class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.password_field :password, :placeholder => 'Password', class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.password_field :password_confirmation, :placeholder => 'Confirm password', class: 'form-control' %>
              </div>
              <div class="login_links">
                 <%= f.submit 'Sign up', :class => 'btn btn-green' %>
                 <%= link_to 'Login', new_user_session_path, class: 'new_link' %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>


  
